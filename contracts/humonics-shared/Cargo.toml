[package]
name = "humonics-shared"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
