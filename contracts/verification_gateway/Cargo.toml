[package]
name = "verification-gateway"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
humonics-shared = { path = "../humonics-shared" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
